{
  "name": "aws-cloudformation-bookshop-test",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:shczhen/aws-cloudformation-bookshop-test.git",
  "author": "czhen <56986964+shczhen@users.noreply.github.com>",
  "license": "MIT",
  "dependencies": {
    "@fastify/aws-lambda": "^3.1.3",
    "@prisma/client": "^4.7.1",
    "fastify": "^4.10.2",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "@types/node": "^18.11.14",
    "esbuild": "^0.16.4",
    "prisma": "^4.7.1",
    "ts-node": "^10.9.1"
  },
  "scripts": {
    "start": "ts-node src/app.ts",
    "prebuild": "rm -rf dist",
    "build": "esbuild src/lambda.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "postbuild": "cd dist && zip -r index.zip index.js*"
  }
}
